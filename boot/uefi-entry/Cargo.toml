[package]
name = "uefi-entry"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
kernel = { path = "../../kernel" }
